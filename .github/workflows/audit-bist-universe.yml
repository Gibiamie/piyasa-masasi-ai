name: Audit BIST Universe

# Verification branch run for the current KAP comparison.
on:
  push:
    branches: [remediation/v30-audit]
    paths:
      - 'scripts/audit-bist-universe.mjs'
      - 'mic/data/market.json'
      - '.github/workflows/audit-bist-universe.yml'
  pull_request:
    branches: [remediation/v30-audit]
    paths:
      - 'scripts/audit-bist-universe.mjs'
      - 'scripts/debug-kap-html.mjs'
      - 'mic/data/market.json'
      - '.github/workflows/audit-bist-universe.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Inspect KAP payload
        run: node scripts/debug-kap-html.mjs

      - name: Run KAP comparison
        run: node scripts/audit-bist-universe.mjs

      - name: Publish job summary
        if: always()
        run: test -f audit-results/bist-universe-audit.md && cat audit-results/bist-universe-audit.md >> "$GITHUB_STEP_SUMMARY" || true

      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bist-universe-audit
          path: audit-results/
          if-no-files-found: ignore
